# areExpressionsEquivalent

Checks if two SLaNg expressions are mathematically equivalent.

## Purpose
Compare expressions by converting both to LaTeX and checking for equality.

## Features
- Round-trip comparison
- Error handling
- Mathematical equivalence
- Type safety

## Usage
```js
const equivalent = areExpressionsEquivalent(expr1, expr2);
// Returns: true if expressions are equivalent
```
